using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using System.Data;
using System.Web.UI;
using System.Web.UI.WebControls;

public partial class NewsCopy : System.Web.UI.Page
{
    protected void Page_Load(object sender, EventArgs e)
    {
        /*
        FerozoNewsReader.NewsReader r = new FerozoNewsReader.NewsReader();

        DataSet news= r.newsGetAll();
        NewsBus n = new NewsBus();
        for (int i = 0; i < news.Tables[0].Rows.Count; i++) {
            n.newsInsert_FromWebSite(news.Tables[0].Rows[i]);
        }
        */
    }
}
